# Advisory Overlay Linked-Docs Review

This ledger records M61 decisions made while reviewing official links cited by
docs-derived advisory overlays. Advisory overlays remain non-official OpenAPI
metadata and should not be treated as provider truth.

## M61 Batch A

| Provider | Overlay | Decision | Notes |
|---|---|---|---|
| Action Network | `action-network-api-v2-overlay.json` | No add | The official v2 docs expose a broad OSDI/HAL API with many action types and helper links. The current overlay already captures the reviewed entry point, people, petitions, forms, events, tags, taggings, and webhooks workflow subset; broader campaign, upload, file, fundraiser, and OSDI relationship resources are deferred to a dedicated expansion to avoid an unfocused generic HAL overlay. |
| ActiveCampaign | `activecampaign-api-v3-overlay.json` | Covered | Reviewed official overview, auth, contact, list, and schema refs. The overlay already covers the focused contacts, lists, campaigns, and deals subset; linked schema/auth pages add metadata rather than additional workflow operations. |
| Acuity Scheduling | `acuity-scheduling-api-v1-overlay.json` | Covered | Reviewed official API reference and quick-start refs. The overlay already captures appointments, appointment retrieval/update/delete, availability, appointment types, calendars, and clients; linked quick-start material is auth/setup guidance. |
| Adalo | `adalo-api-overlay.json` | Covered | Reviewed official API, collections, and push-notification refs. The overlay already includes collection records and notification creation; remaining docs describe app-specific collection schemas and UI setup. |
| Adobe Acrobat Sign | `adobe-acrobat-sign-api-overlay.json` | No add | Official SDK docs point at resource-specific Swagger 1.2 JSON files and generation instructions. The overlay intentionally keeps a conservative base URI and agreement read subset; broader SDK resources should be imported from their own official JSON modules instead of hand-expanded into one advisory overlay. |
| Affinity | `affinity-v1-api-overlay.json` | Covered | Reviewed official V1 API docs and API-key support ref. The overlay already includes the main REST-shaped CRM/list, field, interaction, note, file, webhook, whoami, and rate-limit resources; linked help is credential-management guidance. |
| AfterShip | `aftership-tracking-api-overlay.json` | Covered | Reviewed Tracking API overview and tracking endpoint refs. The overlay covers the courier and tracking lookup subset used by workflow enrichment; broader tracking mutation flows remain outside the reviewed read-oriented subset. |
| Agile CRM | `agile-crm-rest-api-overlay.json` | Covered | Reviewed official REST API docs and GitHub docs source. The overlay already covers the focused CRM resources used for contacts, campaigns, opportunities, events, tasks, tracks, tickets, search, and properties. |
| Aircall | `aircall-public-api-overlay.json` | Expanded | Official Aircall API references include a Webhook API alongside users, calls, contacts, and availability. Added conservative webhook list/create/get/update/delete operations to the existing overlay. |
| Airtable | `airtable-web-api-overlay.json` | Expanded | The support Webhooks API overview links to official Webhooks API developer docs and describes real-time base-change notifications. Added conservative webhook management and payload retrieval operations alongside the existing table/record and base-schema operations. |
| APITemplate.io | `apitemplate-io-api-overlay.json` | Covered | Reviewed official API docs. The overlay already captures the focused PDF/image creation and template listing workflow subset; linked material is endpoint-local parameter documentation. |
| Apollo.io | `apollo-api-overlay.json` | Covered | Reviewed official llms index and endpoint-level authentication/search refs. The overlay already covers the selected people and organization search operations from official fragments; broader Apollo APIs need separate scoped review. |
| Autopilot | `autopilot-api-overlay.json` | Covered | Reviewed official Apiary docs and help article. The overlay covers the focused contacts and lists subset; linked help explains API-key usage and product migration context. |
| BambooHR | `bamboohr-api-v1-overlay.json` | No add | Official docs link to OAuth, API-key, field, webhooks, SDK, and reference material. The overlay already covers the reviewed employee/directory/fields/reports/time-off subset; webhook and OAuth token workflows are admin/app setup surfaces and are deferred until a dedicated BambooHR expansion. |
| Bannerbear | `bannerbear-api-v2-overlay.json` | Covered | Reviewed official v2 docs and help ref. The overlay already includes auth, projects, templates, images, collections, screenshots, and videos; linked sync-host and webhook callback notes are behavior variants, not separate operations for this advisory subset. |
| Beeminder | `beeminder-api-v1-overlay.json` | Covered | Reviewed official API reference, auth-token endpoint, and app-registration refs. The overlay already covers users, goals, datapoints, charges, graph refresh, and ratchet operations; linked app registration is credential setup. |
| Bitwarden | `bitwarden-public-api-overlay.json` | Covered | Reviewed official Public API docs. The overlay already covers organization info, collections, groups, members, and events; OAuth token exchange remains security metadata rather than endpoint coverage in apitools. |
| Bubble | `bubble-api-overlay.json` | Covered | Reviewed official Bubble API, Data API, Workflow API, and app-generated Swagger guidance. The overlay stays generic because actual object types, workflow names, hosts, and schemas are app-specific and can be exported through Bubble's own Swagger when enabled. |
| Calendly | `calendly-public-api-overlay.json` | Covered | Reviewed official API docs, getting-started, auth, OAuth app, and Stoplight refs. The overlay already covers event types, events, invitees, scheduling links, current user, and webhook subscriptions. |
| Checkr | `checkr-api-overlay.json` | Covered | Reviewed official Redoc-rendered docs. The overlay intentionally remains read-oriented around account, candidates, packages, and reports; broader candidate/report creation and screening operations carry higher workflow and PII risk and remain outside this advisory subset. |
| Cisco Webex | `cisco-webex-api-overlay.json` | Covered | Reviewed official messaging API docs and integration docs. The overlay already includes rooms, messages, memberships, current user, webhooks, attachment actions, and recordings for the reviewed chat automation subset. |
| Clearbit | `clearbit-api-overlay.json` | Covered | Reviewed official support API category and linked API-key/autocomplete/Prospector refs. The overlay keeps the narrow person search and combined enrichment subset; unsupported or legacy API docs are not added. |
| Clockify | `clockify-api-v1-overlay.json` | Expanded | Official Clockify API docs include webhook endpoints alongside the time-tracking resources already covered. Added conservative workspace webhook list/create/get/update/delete operations. |
| Cockpit | `cockpit-api-overlay.json` | Covered | Reviewed official core API docs. The overlay already covers assets, collection content, tree content, item mutation, and singletons; remaining model schemas and host details are self-hosted/app-specific. |
| CoinGecko | `coingecko-api-v3-overlay.json` | Covered | Reviewed official reference introduction, authentication, endpoint overview, and simple price refs. The overlay already covers the focused market-data subset; Pro/Demo auth variants remain security metadata. |
| Contentful | `contentful-management-api-overlay.json` | No add | The linked Content Delivery API is official and workflow-relevant, but it uses a different host, read-only semantics, and delivery-token behavior over paths that overlap the Content Management API. Adding it to the same Management API overlay would conflate API surfaces; keep the source ref for provenance and defer a separate delivery-focused overlay if needed. |

## M61 Batch B

| Provider | Overlay | Decision | Notes |
|---|---|---|---|
| Copper | `copper-developer-api-overlay.json` | Covered | Reviewed official developer API, authentication, and request refs. The overlay already covers the focused users, leads, people, companies, opportunities, projects, tasks, activities, search, and webhook subset; linked auth/request material adds credential and payload guidance rather than additional operations. |
| Cortex | `cortex-api-overlay.json` | Covered | Reviewed official Cortex API guide. The overlay already covers the focused analyzer, job, organization, and responder subset; linked docs describe the same API guide and deployment-specific behavior. |
| Currents | `currents-api-overlay.json` | Covered | Reviewed official API, auth, and resources refs. The overlay already captures projects, runs, tests, spec files, actions, signatures, instances, and webhooks; linked pages add endpoint-local parameters and auth notes. |
| Databricks | `databricks-workspace-rest-overlay.json` | No add | Reviewed workspace REST, jobs, and PAT auth refs. The overlay keeps a focused jobs, clusters, workspace, and DBFS subset with operator-supplied workspace host; the broader Databricks REST estate should be imported or expanded as separate scoped work instead of merged wholesale into this advisory overlay. |
| Demio | `demio-api-overlay.json` | Covered | Reviewed official Apiary docs and limitations ref. The overlay already covers the supported events, registrations, and reports subset; linked limitations and auth material are not separate operations. |
| DHL | `dhl-shipment-tracking-overlay.json` | No add | Reviewed Shipment Tracking - Unified docs and the linked API selector. The overlay covers the documented tracking endpoint; the selector helps choose other DHL products but is not operation-level documentation for this overlay. |
| Disqus | `disqus-api-overlay.json` | Covered | Reviewed official API and auth refs. The overlay already captures the focused forums, threads, posts, details, and listing subset; linked pages add auth and endpoint-local detail. |
| Drift | `drift-platform-api-overlay.json` | Covered | Reviewed official platform API, auth, and scope refs. The overlay already covers the focused contacts, conversations, and users subset; linked scope docs refine security metadata rather than adding operations. |
| Dropbox | `dropbox-core-api-overlay.json` | No add | Reviewed the official Dropbox API spec repository, HTTP docs, and OAuth guide. The overlay remains a focused core files, sharing, and users subset; the broader official Stone/OpenAPI-shaped spec should be consumed as its own machine-readable artifact path rather than hand-copied into this advisory overlay. |
| Dropcontact | `dropcontact-api-overlay.json` | Covered | Reviewed official developer docs. The overlay covers the batch enrichment submission and result polling workflow; linked docs do not expose additional workflow operations for this reviewed subset. |
| ERPNext | `erpnext-rest-api-overlay.json` | Covered | Reviewed ERPNext and Frappe REST/auth refs. The overlay intentionally captures generic DocType/resource and whitelisted-method patterns because concrete DocTypes are site/app-specific. |
| Eventbrite | `eventbrite-platform-api-v3-overlay.json` | Covered | Reviewed platform API and auth refs. The overlay already covers users, organizations, events, attendees, venues, and ticket classes; linked docs add auth/setup context. |
| Facebook Graph | `facebook-graph-api-overlay.json` | Covered | Reviewed Graph API, Pages API, and Webhooks refs. The overlay already covers the focused profile, page, feed, photos, comments, likes, conversations, page subscriptions, and app subscription subset. |
| Facebook Lead Ads | `facebook-lead-ads-overlay.json` | Covered | Reviewed Lead Ads retrieval and webhook refs. The overlay already covers ad account and page leadgen form discovery, form leads, lead lookup, and app/page subscription operations. |
| FileMaker | `filemaker-data-api-overlay.json` | Covered | Reviewed Claris Data API reference. The overlay already covers sessions, layouts, records, scripts, and containers; host, database, layout, and script names remain operator supplied. |
| Flow | `flow-api-overlay.json` | Covered | Reviewed official Flow API docs. The overlay already covers tasks, access tokens, and integration webhook operations; linked docs do not expose additional focused operations. |
| Form.io | `formio-api-overlay.json` | Covered | Reviewed official API docs. The overlay already covers projects, forms, submissions, and roles; cloud/self-hosted host and project aliases remain operator supplied. |
| Formstack | `formstack-api-v2-overlay.json` | Covered | Reviewed official Forms API v2 docs, API overview, and auth refs. The overlay already covers forms, fields, submissions, folders, and webhooks. |
| Freshdesk | `freshdesk-api-v2-overlay.json` | Covered | Reviewed Freshdesk API v2 docs and support refs. The overlay already captures tickets, contacts, companies, and agents for the focused support workflow subset. |
| Freshservice | `freshservice-api-v2-overlay.json` | Covered | Reviewed Freshservice API v2 docs and support refs. The overlay already captures tickets, problems, changes, releases, requesters, agents, assets, groups, departments, and service catalog endpoints. |
| Freshworks CRM | `freshworks-crm-api-overlay.json` | Covered | Reviewed official Freshworks CRM API docs. The overlay already covers contacts, sales accounts, deals, and tasks; account domain remains operator supplied. |
| GetResponse | `getresponse-api-v3-overlay.json` | Covered | Reviewed official API v3 and auth refs. The overlay already covers campaigns, contacts, newsletters, and tags; linked docs add auth and endpoint-local metadata. |
| Ghost | `ghost-admin-api-overlay.json` | No add | Reviewed Admin API and linked Content API docs. The overlay remains Admin API focused with posts, pages, tags, users, and members; the Content API has different public delivery semantics and token behavior and should be represented separately if needed. |
| Gong | `gong-public-api-overlay.json` | Covered | Reviewed public help docs and upload-calls material. The overlay keeps the narrow users and call upload/media subset; broader API reference material remains account-session gated and is not a source for advisory expansion. |
| Google Calendar | `google-calendar-v3-overlay.json` | Expanded | Reviewed official Discovery and REST refs. Added conservative free/busy query, event watch, and channel stop operations, with source refs for the specific operation pages. |
| Google Sheets | `google-sheets-v4-overlay.json` | Covered | Reviewed official Discovery, REST, and scopes refs. The overlay already covers spreadsheets and values get/update/append/batch operations; linked guide material is operation-local usage guidance rather than a separate related API surface. |
